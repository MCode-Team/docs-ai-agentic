# 📚 คำศัพท์ทางเทคนิคและ AI (Technical Glossary)

> เอกสารนี้รวบรวมคำศัพท์ทางเทคนิคและ AI ทั้งหมดที่ใช้ในโปรเจค **AI Agentic Docs** พร้อมคำอธิบายที่เข้าใจง่ายและตัวอย่างประกอบ

---

## 🤖 AI และ Machine Learning

### 1. LLM (Large Language Model)
**ภาษาไทย:** โมเดลภาษาขนาดใหญ่

**คำอธิบาย:** โปรแกรม AI ที่ถูกฝึกด้วยข้อมูลข้อความจำนวนมหาศาล ทำให้สามารถเข้าใจและสร้างข้อความที่เป็นธรรมชาติได้

**ตัวอย่าง:** 
- ChatGPT, Claude, Gemini คือตัวอย่างของ LLM
- เหมือนกับ "นักอ่านตัวยง" ที่อ่านหนังสือล้านๆ เล่ม จนสามารถเขียนตอบคำถามได้เหมือนมนุษย์

---

### 2. Embedding
**ภาษาไทย:** การแปลงเป็นเวกเตอร์

**คำอธิบาย:** การแปลงข้อความให้เป็นตัวเลข (เวกเตอร์) เพื่อให้คอมพิวเตอร์เข้าใจ "ความหมาย" ของข้อความได้

**ตัวอย่าง:**
- คำว่า "แมว" และ "สุนัข" จะมีเวกเตอร์ที่ใกล้กันเพราะเป็นสัตว์เลี้ยงเหมือนกัน
- แต่ "แมว" กับ "รถยนต์" จะมีเวกเตอร์ที่ห่างกัน
- เหมือนกับการให้พิกัด GPS กับทุกคำ คำที่ความหมายคล้ายกันจะอยู่ใกล้กัน

---

### 3. Token
**ภาษาไทย:** โทเค็น/หน่วยข้อความ

**คำอธิบาย:** ชิ้นส่วนของข้อความที่ AI ใช้ประมวลผล อาจเป็นคำ ส่วนของคำ หรืออักขระ

**ตัวอย่าง:**
- ประโยค "Hello world" อาจแบ่งเป็น 2 tokens: ["Hello", " world"]
- ภาษาไทย "สวัสดี" อาจแบ่งเป็น 3-4 tokens เพราะ AI ไม่คุ้นเคยกับภาษาไทยเท่าอังกฤษ
- เหมือนกับการแบ่งประโยคเป็นคำๆ เพื่อให้ AI อ่านทีละนิด

---

### 4. Prompt
**ภาษาไทย:** คำสั่งหรือข้อความนำเข้า

**คำอธิบาย:** ข้อความที่เราพิมพ์ให้ AI เพื่อบอกว่าต้องการให้ทำอะไร

**ตัวอย่าง:**
- "ช่วยสรุปเอกสารนี้" คือ prompt
- "เขียนโค้ด Python คำนวณหา Fibonacci" คือ prompt
- เหมือนกับการบอกคำสั่งกับผู้ช่วยว่าต้องการให้ทำอะไร

---

### 5. Hallucination
**ภาษาไทย:** อาการหลอน/สร้างข้อมูลเท็จ

**คำอธิบาย:** เมื่อ AI สร้างข้อมูลที่ดูน่าเชื่อถือแต่ไม่ถูกต้องตามความเป็นจริง

**ตัวอย่าง:**
- ถามว่า "ใครเขียนหนังสือ XYZ" แล้ว AI ตอบชื่อผู้เขียนที่ไม่มีอยู่จริง
- เหมือนกับคนที่จำอะไรผิดแต่ตอบอย่างมั่นใจ
- **Zerank/Reranking** ช่วยลด Hallucination โดยให้ข้อมูลที่ถูกต้องกับ AI

---

## 🔍 RAG และระบบค้นหา

### 6. RAG (Retrieval-Augmented Generation)
**ภาษาไทย:** การสร้างคำตอบโดยมีการดึงข้อมูลประกอบ

**คำอธิบาย:** เทคนิคที่รวม "การค้นหาข้อมูล" + "การสร้างคำตอบด้วย AI" เข้าด้วยกัน ทำให้ AI ตอบได้แม่นยำขึ้นเพราะมีข้อมูลจริงประกอบ

**ตัวอย่าง:**
- แทนที่จะถาม AI เปล่าๆ ระบบจะ "ค้นหา" เอกสารที่เกี่ยวข้องก่อน แล้วส่งให้ AI อ่านก่อนตอบ
- เหมือนกับการให้นักเรียนเปิดหนังสือดูก่อนตอบข้อสอบ แทนที่จะตอบจากความจำอย่างเดียว

```
คำถามผู้ใช้ → ค้นหาเอกสาร → เอกสารที่เกี่ยวข้อง → AI สร้างคำตอบ
```

---

### 7. Vector Database
**ภาษาไทย:** ฐานข้อมูลเวกเตอร์

**คำอธิบาย:** ฐานข้อมูลพิเศษที่จัดเก็บข้อมูลในรูปแบบตัวเลข (embedding) และค้นหาข้อมูลที่ "คล้ายกัน" ได้

**ตัวอย่าง:**
- **pgvector** คือ extension ของ PostgreSQL ที่ทำให้เป็น Vector Database ได้
- ถามว่า "วิธีติดตั้ง" ระบบจะค้นหาเอกสารที่มีเนื้อหาเกี่ยวกับ "การติดตั้ง", "setup", "installation" ได้ทั้งหมด
- เหมือนกับห้องสมุดที่จัดหมวดหมู่หนังสือตาม "ความหมาย" ไม่ใช่แค่ตัวอักษร

---

### 8. Chunking
**ภาษาไทย:** การแบ่งข้อมูลเป็นชิ้นย่อย

**คำอธิบาย:** การแบ่งเอกสารยาวๆ ออกเป็นชิ้นเล็กๆ เพื่อให้จัดเก็บและค้นหาได้ง่าย

**ตัวอย่าง:**
- เอกสาร 10 หน้า แบ่งเป็น 20 chunks ชิ้นละ ~900 ตัวอักษร
- เมื่อผู้ใช้ถามคำถาม ระบบจะค้นหา chunks ที่เกี่ยวข้องเท่านั้น ไม่ต้องอ่านทั้งเอกสาร
- เหมือนกับการแบ่งพจนานุกรมเป็นแต่ละหน้า แทนที่จะค้นทั้งเล่ม

---

### 9. Semantic Search
**ภาษาไทย:** การค้นหาเชิงความหมาย

**คำอธิบาย:** การค้นหาที่เข้าใจ "ความหมาย" ของคำ ไม่ใช่แค่จับคู่ตัวอักษร

**ตัวอย่าง:**
- ค้นหา "วิธีเริ่มต้นใช้งาน" จะเจอเอกสาร "Getting Started" ได้ ถึงแม้คำไม่ตรงกัน
- ต่างจากการค้นหาแบบเดิมที่ต้องพิมพ์คำที่ตรงกันเป๊ะๆ
- เหมือนกับการถามบรรณารักษ์ที่เข้าใจสิ่งที่คุณต้องการ แทนที่จะค้นจากชื่อหนังสือเท่านั้น

---

### 10. Reranking
**ภาษาไทย:** การจัดอันดับใหม่

**คำอธิบาย:** หลังจากค้นหาข้อมูลมาแล้ว มีการให้คะแนนความเกี่ยวข้องใหม่ เพื่อเลือกเฉพาะข้อมูลที่ตรงจุดที่สุด

**ตัวอย่าง:**
- ค้นหาได้ 20 ผลลัพธ์ → **Zerank** จะให้คะแนนใหม่ → ได้ 5 ผลลัพธ์ที่ดีที่สุด
- ช่วยลด "noise" และให้ AI ได้รับข้อมูลที่แม่นยำขึ้น
- เหมือนกับการคัดหนังสือที่ดีที่สุดจากหลายๆ เล่มที่เกี่ยวข้อง

---

### 11. Data Dictionary
**ภาษาไทย:** พจนานุกรมข้อมูล

**คำอธิบาย:** เอกสารที่อธิบายโครงสร้างฐานข้อมูล - ตาราง คอลัมน์ และความหมายของแต่ละฟิลด์

**ตัวอย่าง:**
- ตาราง `users` มีคอลัมน์ `email` (อีเมลผู้ใช้), `created_at` (วันที่สมัคร)
- ช่วยให้ AI เข้าใจโครงสร้างฐานข้อมูลและสามารถ query ได้ถูกต้อง
- เหมือนกับหนังสือคู่มือที่อธิบายความหมายของแต่ละช่องในแบบฟอร์ม

---

## 🤖 Agent และ Agentic AI

### 12. Agentic AI / AI Agent
**ภาษาไทย:** AI ที่ทำงานเป็นตัวแทน

**คำอธิบาย:** AI ที่สามารถ "คิด วางแผน ลงมือทำ และปรับตัว" ได้เอง ไม่ใช่แค่รับคำสั่งและตอบ

**ตัวอย่าง:**
- ถาม "หายอดขายเดือนนี้แล้วสร้างกราฟ" → Agent จะ:
  1. วางแผนว่าต้องดึงข้อมูลจากไหน
  2. เรียกใช้ tool ดึงข้อมูล
  3. สร้างกราฟ
  4. ส่งผลลัพธ์กลับ
- เหมือนกับเลขานุการที่รับมอบหมายงาน แล้วจัดการเองได้ทั้งหมด

---

### 13. Agent Loop
**ภาษาไทย:** ลูปการทำงานของ Agent

**คำอธิบาย:** วงจรการทำงานแบบวนซ้ำ: Plan → Execute → Observe → Reflect → Repeat

**ตัวอย่าง:**
```
1. Plan: วางแผนว่าจะทำอะไร
2. Execute: ลงมือทำตามแผน
3. Observe: ดูผลลัพธ์
4. Reflect: ประเมินว่าสำเร็จหรือไม่
5. กลับไปข้อ 1 ถ้ายังไม่เสร็จ
```
- เหมือนกับคนที่ทำงาน → ตรวจสอบ → แก้ไข → ทำต่อ จนเสร็จ

---

### 14. Planner
**ภาษาไทย:** ระบบวางแผน

**คำอธิบาย:** ส่วนประกอบของ Agent ที่ทำหน้าที่วิเคราะห์คำถามและสร้างแผนการทำงาน

**ตัวอย่าง:**
- คำถาม: "เปรียบเทียบยอดขายเดือนนี้กับเดือนก่อน"
- Planner สร้างแผน:
  1. `tool`: ดึงข้อมูลเดือนนี้
  2. `tool`: ดึงข้อมูลเดือนก่อน
  3. `think`: วิเคราะห์ความแตกต่าง
  4. `answer`: ตอบคำถาม
- เหมือนกับหัวหน้าทีมที่วางแผนงานก่อนลงมือทำ

---

### 15. Reflector
**ภาษาไทย:** ระบบประเมินผล

**คำอธิบาย:** ส่วนประกอบของ Agent ที่ตรวจสอบผลลัพธ์และตัดสินใจว่าควรทำต่อหรือปรับแผน

**ตัวอย่าง:**
- หลังเรียก tool แล้ว Reflector จะประเมิน:
  - ได้ข้อมูลครบหรือไม่?
  - ต้องดึงข้อมูลเพิ่มไหม?
  - ควรเปลี่ยนวิธีการหรือไม่?
- เหมือนกับ QA ที่ตรวจสอบว่างานตรงตามที่ต้องการหรือไม่

---

### 16. Tool / Function Calling
**ภาษาไทย:** การเรียกใช้เครื่องมือ

**คำอธิบาย:** ความสามารถของ AI ในการเรียกใช้ฟังก์ชันหรือ API ภายนอก

**ตัวอย่าง:**
- AI ต้องการหาสภาพอากาศ → เรียก `getWeather("Bangkok")`
- AI ต้องการคำนวณ → เรียก `executeCode("import math; result = math.sqrt(16)")`
- เหมือนกับการให้ AI ใช้เครื่องมือต่างๆ เช่น เครื่องคิดเลข, สมุดโทรศัพท์, แผนที่

---

### 17. Human-in-the-Loop (HITL)
**ภาษาไทย:** มนุษย์เป็นส่วนหนึ่งในกระบวนการ

**คำอธิบาย:** การออกแบบระบบให้มนุษย์สามารถอนุมัติหรือปฏิเสธการกระทำของ AI ก่อนที่จะเกิดขึ้นจริง

**ตัวอย่าง:**
- AI ต้องการลบไฟล์ → ระบบถามผู้ใช้ "อนุมัติ/ปฏิเสธ" ก่อนลบ
- AI จะส่งอีเมล → ผู้ใช้ต้องกด "Approve" ก่อน
- เหมือนกับระบบที่ต้องได้รับอนุมัติจากผู้จัดการก่อนดำเนินการ

---

### 18. Memory System
**ภาษาไทย:** ระบบความจำ

**คำอธิบาย:** ระบบที่ช่วยให้ AI จำบริบทการสนทนาและข้อมูลสำคัญข้ามเซสชัน

**ตัวอย่าง:**
- **Short-term Memory:** จำสิ่งที่คุยกันในเซสชันนี้
- **Long-term Memory:** จำว่าผู้ใช้ชอบอะไร ใช้ภาษาอะไร
- **User Facts:** จำข้อมูลเฉพาะผู้ใช้ เช่น "ผู้ใช้เป็นนักพัฒนา Python"
- เหมือนกับเลขานุการที่จำความชอบของหัวหน้าได้

---

### 19. Re-planning
**ภาษาไทย:** การวางแผนใหม่

**คำอธิบาย:** เมื่อแผนเดิมล้มเหลวหรือได้ข้อมูลใหม่ Agent จะสร้างแผนใหม่โดยอัตโนมัติ

**ตัวอย่าง:**
- แผนเดิม: ดึงข้อมูลจาก API A → API A ล้มเหลว
- Re-plan: ลองดึงจาก API B แทน หรือแจ้งผู้ใช้ว่าข้อมูลไม่พร้อม
- เหมือนกับคนที่มีแผนสำรองเมื่อแผนหลักไม่สำเร็จ

---

## 💻 Tech Stack และเครื่องมือ

### 20. Next.js
**ภาษาไทย:** เฟรมเวิร์ค Next.js

**คำอธิบาย:** เฟรมเวิร์ค React สำหรับสร้างเว็บแอปพลิเคชัน รองรับ Server-Side Rendering และ API Routes

**ตัวอย่าง:**
- โปรเจคนี้ใช้ Next.js 15 กับ App Router
- สร้างหน้าเว็บ (Frontend) และ API (Backend) ในที่เดียว
- เหมือนกับ "กล่องเครื่องมือสำเร็จรูป" สำหรับสร้างเว็บไซต์

---

### 21. Vercel AI SDK
**ภาษาไทย:** ชุดเครื่องมือ AI จาก Vercel

**คำอธิบาย:** ไลบรารี่สำหรับเชื่อมต่อกับ AI Models และจัดการ Streaming Responses

**ตัวอย่าง:**
- ใช้ `generateText()` เพื่อสร้างข้อความจาก AI
- ใช้ `streamText()` เพื่อแสดงผลแบบ real-time (ตัวอักษรทีละตัว)
- รองรับ OpenAI, Anthropic, Google และอื่นๆ

---

### 22. pgvector
**ภาษาไทย:** ส่วนขยาย pgvector

**คำอธิบาย:** Extension สำหรับ PostgreSQL ที่เพิ่มความสามารถในการจัดเก็บและค้นหา Vector Embeddings

**ตัวอย่าง:**
```sql
-- สร้างคอลัมน์เก็บ embedding
CREATE TABLE docs_chunks (
  id SERIAL PRIMARY KEY,
  content TEXT,
  embedding vector(1536)
);

-- ค้นหาเอกสารที่ใกล้เคียง
SELECT * FROM docs_chunks
ORDER BY embedding <-> query_embedding
LIMIT 5;
```
- เหมือนกับการเพิ่ม "GPS" ให้ฐานข้อมูลสามารถค้นหาตาม "ตำแหน่งความหมาย"

---

### 23. Streaming Response
**ภาษาไทย:** การตอบกลับแบบสตรีม

**คำอธิบาย:** การส่งข้อมูลกลับทีละส่วนแบบ real-time แทนที่จะรอให้เสร็จทั้งหมดก่อน

**ตัวอย่าง:**
- เมื่อ AI ตอบคำถาม ข้อความจะปรากฏทีละคำเหมือนคนพิมพ์
- ผู้ใช้ไม่ต้องรอนานจนกว่า AI จะคิดเสร็จ
- เหมือนกับการอ่านข้อความที่คนอีกคนกำลังพิมพ์ตอบ

---

### 24. MCP (Model Context Protocol)
**ภาษาไทย:** โปรโตคอลบริบทโมเดล

**คำอธิบาย:** มาตรฐานในการเชื่อมต่อ AI กับ Tools และแหล่งข้อมูลภายนอก

**ตัวอย่าง:**
- เหมือน "USB-C" ของ AI - เสียบปลั๊กเดียวใช้ได้กับทุกอุปกรณ์
- ผู้พัฒนา Tools สร้าง MCP Server ครั้งเดียว ใช้ได้กับ AI ทุกตัวที่รองรับ MCP
- โครงสร้าง: MCP Host (AI App) → MCP Client → MCP Server (External Tools)

---

### 25. Sandbox
**ภาษาไทย:** สภาพแวดล้อมแยกส่วน

**คำอธิบาย:** พื้นที่ปลอดภัยสำหรับรันโค้ดโดยไม่กระทบระบบหลัก

**ตัวอย่าง:**
- AI ต้องการรัน Python code → รันใน Sandbox (Docker Container)
- ถ้าโค้ดมีปัญหาหรืออันตราย จะไม่กระทบเซิร์ฟเวอร์หลัก
- เหมือนกับ "ห้องทดลอง" ที่แยกออกจากห้องทำงานหลัก

---

### 26. Docker / Container
**ภาษาไทย:** คอนเทนเนอร์

**คำอธิบาย:** เทคโนโลยีที่บรรจุแอปพลิเคชันและ dependencies ทั้งหมดไว้ในกล่องเดียว รันได้ทุกที่

**ตัวอย่าง:**
- `docker-compose up -d` = เปิดระบบทั้งหมด (Web, Database, Zerank, Sandbox)
- ไม่ต้องติดตั้งโปรแกรมแยกต่างหาก เพราะทุกอย่างอยู่ใน container แล้ว
- เหมือนกับ "กล่องพร้อมใช้" ที่ย้ายไปรันที่ไหนก็ได้

---

## 📊 Database และข้อมูล

### 27. Schema
**ภาษาไทย:** โครงสร้างฐานข้อมูล

**คำอธิบาย:** การกำหนดโครงสร้างของตาราง คอลัมน์ และความสัมพันธ์ในฐานข้อมูล

**ตัวอย่าง:**
```sql
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  email VARCHAR(255) UNIQUE,
  name TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```
- เหมือนกับ "พิมพ์เขียว" ของฐานข้อมูล

---

### 28. Schema Introspection
**ภาษาไทย:** การอ่านโครงสร้างฐานข้อมูลอัตโนมัติ

**คำอธิบาย:** กระบวนการที่ระบบอ่านโครงสร้างฐานข้อมูลเพื่อสร้าง Data Dictionary โดยอัตโนมัติ

**ตัวอย่าง:**
- สคริปต์ `ingest-db-dictionary.ts` อ่านจาก `information_schema` ของ PostgreSQL
- ได้ข้อมูลตาราง คอลัมน์ และ comments ทั้งหมด
- เหมือนกับการ "สแกน" ห้องสมุดเพื่อทำรายการหนังสือทั้งหมด

---

### 29. Ingestion
**ภาษาไทย:** การนำเข้าข้อมูล

**คำอธิบาย:** กระบวนการแปลงและนำข้อมูลดิบเข้าสู่ระบบ (เช่น การแปลงเอกสารเป็น Vector)

**ตัวอย่าง:**
- `npm run ingest:docs` = นำเข้าไฟล์ .mdx → แบ่ง chunk → สร้าง embedding → เก็บใน database
- `npm run ingest:dict` = นำเข้าโครงสร้าง DB → สร้างคำอธิบาย → สร้าง embedding
- เหมือนกับการแปลงหนังสือเป็นรูปแบบดิจิทัลและจัดหมวดหมู่

---

## 🎨 UI และ UX

### 30. Streaming UI
**ภาษาไทย:** อินเตอร์เฟสแบบสตรีม

**คำอธิบาย:** UI ที่แสดงข้อมูลแบบ real-time ทันทีที่ได้รับ

**ตัวอย่าง:**
- ข้อความ AI ปรากฏทีละคำ ไม่ต้องรอจนเสร็จ
- แสดงขั้นตอน "กำลังคิด...", "กำลังค้นหา...", "กำลังตอบ..."
- ทำให้ผู้ใช้รู้สึกว่าระบบกำลังทำงาน ไม่ใช่ค้าง

---

### 31. Citation / Source Reference
**ภาษาไทย:** การอ้างอิงแหล่งที่มา

**คำอธิบาย:** การแสดงลิงก์กลับไปยังเอกสารต้นทางที่ AI ใช้ในการตอบคำถาม

**ตัวอย่าง:**
- คำตอบ AI: "ตาม Getting Started Guide [1]..."
- `[1]` เป็นลิงก์ไปยังหน้าเอกสาร
- ช่วยให้ผู้ใช้ตรวจสอบความถูกต้องได้

---

### 32. Process Visualization
**ภาษาไทย:** การแสดงกระบวนการทำงาน

**คำอธิบาย:** การแสดงให้ผู้ใช้เห็นว่า AI กำลังทำอะไรในแต่ละขั้นตอน

**ตัวอย่าง:**
- แสดง "🔍 กำลังค้นหาเอกสาร..."
- แสดง "🧠 กำลังวิเคราะห์..."
- แสดง "✅ เรียกใช้ getSalesSummary สำเร็จ"
- ทำให้ผู้ใช้เข้าใจว่า AI ทำอะไร ไม่ใช่กล่องดำลึกลับ

---

## 📐 สถาปัตยกรรมระบบ

### 33. Hybrid Architecture
**ภาษาไทย:** สถาปัตยกรรมแบบลูกผสม

**คำอธิบาย:** การออกแบบที่รวมหลายวิธีการเข้าด้วยกัน เพื่อให้เหมาะกับแต่ละสถานการณ์

**ตัวอย่าง:**
- **Agentic Mode:** สำหรับคำถามซับซ้อนที่ต้องวางแผนและใช้หลาย tools
- **Simple Mode:** สำหรับคำถามง่ายๆ ที่ต้องการความเร็ว
- ระบบเลือกโหมดอัตโนมัติตามความซับซ้อนของคำถาม

---

### 34. API Route / Endpoint
**ภาษาไทย:** เส้นทาง API

**คำอธิบาย:** URL ที่ใช้สื่อสารระหว่าง Frontend และ Backend

**ตัวอย่าง:**
- `/api/ask-ai` = Endpoint สำหรับส่งคำถามให้ AI
- Frontend ส่ง POST request ไปที่ endpoint นี้
- Backend ประมวลผลและตอบกลับ

---

### 35. App Router (Next.js)
**ภาษาไทย:** ระบบเส้นทางแบบ App

**คำอธิบาย:** รูปแบบใหม่ของ Next.js ในการจัดการหน้าเว็บและ API โดยใช้โครงสร้างโฟลเดอร์

**ตัวอย่าง:**
```
app/
├── page.tsx           → หน้าแรก (/)
├── api/
│   └── ask-ai/
│       └── route.ts   → API (/api/ask-ai)
└── docs/
    └── page.tsx       → หน้าเอกสาร (/docs)
```

---

## 🔧 คำศัพท์อื่นๆ ที่พบบ่อย

### 36. MDX
**ภาษาไทย:** เอ็มดีเอกซ์

**คำอธิบาย:** รูปแบบไฟล์ที่รวม Markdown + JSX ทำให้เขียนเอกสารได้พร้อมกับ Component React

**ตัวอย่าง:**
```mdx
# หัวข้อ

ข้อความปกติ markdown

<CustomAlert type="warning">
  นี่คือ React Component ใน markdown!
</CustomAlert>
```

---

### 37. Tailwind CSS
**ภาษาไทย:** เทลวินด์ ซีเอสเอส

**คำอธิบาย:** CSS Framework ที่ใช้ class utilities แทนการเขียน CSS แยก

**ตัวอย่าง:**
```html
<div class="bg-blue-500 p-4 rounded-lg shadow-md">
  สีฟ้า มี padding มุมโค้ง มีเงา
</div>
```

---

### 38. TypeScript
**ภาษาไทย:** ไทป์สคริปต์

**คำอธิบาย:** JavaScript ที่มีระบบ Type เพิ่มเติม ช่วยตรวจจับ error ก่อนรันโปรแกรม

**ตัวอย่าง:**
```typescript
interface User {
  id: number;
  name: string;
  email: string;
}

function greet(user: User): string {
  return `Hello, ${user.name}!`;
}
```

---

## 📖 สรุป

| หมวดหมู่ | คำศัพท์สำคัญ |
|---------|-------------|
| **AI พื้นฐาน** | LLM, Embedding, Token, Prompt, Hallucination |
| **RAG/Search** | RAG, Vector Database, Chunking, Semantic Search, Reranking |
| **Agent** | Agentic AI, Agent Loop, Planner, Reflector, Tool Calling, HITL |
| **Tech Stack** | Next.js, Vercel AI SDK, pgvector, Streaming, MCP, Sandbox |
| **Database** | Schema, Introspection, Ingestion, Data Dictionary |
| **UI/UX** | Streaming UI, Citation, Process Visualization |

---

> 💡 **เคล็ดลับ:** ถ้าต้องการเข้าใจคำศัพท์เพิ่มเติม ลองถาม AI Assistant ในโปรเจคนี้ได้เลย!
